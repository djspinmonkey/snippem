<% @title = "Snippets for #{@user}" %>

<% @snippets.each do |s| %>
  <% div_for(s) do %>
    Date: <%= s.created_at %> <br>
    Title: <%= s.title %> <br>

    <p> <pre><%= wrap_text(s.contents, 90) %></pre> </p>
  <% end %>

<% end %>
